# Scrinium

Scrinium is an **ODM framework** (Object-Documental Mapper) for **MongoDB** on .NET, oriented to Asp.NET Core applications. It maps domain objects to documents, manages denormalized references between documents (with automatic dependency updates), versioned document schemas on the same collection, and data migrations. It is a set of **libraries**, not an application.

## Build, run, test

Libraries multi-target **.NET 8, 9 and 10**; the test projects target **.NET 10 only**. `TreatWarningsAsErrors=true` and `AnalysisMode=AllEnabledByDefault` are set everywhere — warnings break the build, on every target framework.

```bash
dotnet restore Scrinium.sln
dotnet build Scrinium.sln -c Release                  # compiles every target framework
dotnet test  Scrinium.sln -c Release                  # runs the xUnit test projects
dotnet test test/Scrinium.Core.UnitTests/Scrinium.Core.UnitTests.csproj    # single project
dotnet test --filter "FullyQualifiedName~DbContextTest"          # single class
dotnet test --filter "FullyQualifiedName~DbContextTest.CanRunExclusiveAccess"  # single test
dotnet run  --project samples/AspNetCoreSample       # sample web app (requires a local MongoDB)
```

Because the libraries compile against the lowest target (`net8.0`), do not use APIs introduced only in a later framework — it will pass locally on `net10.0` and fail the build on `net8.0`. A green `Scrinium.sln` build means all frameworks compiled.

Integration tests (`test/Scrinium.IntegrationTests`) need a real MongoDB instance supporting transactions: they use the `SCRINIUM_TEST_DB_URL` environment variable when set (CI runs a `mongo` container initiated as single node replica set), otherwise they spawn a throwaway local `mongod` process as a single node replica set (the binary must be on `PATH`).

Versioning is computed by **GitVersion** (no manual version bumps). CI (`.github/workflows/`) publishes unstable packages to MyGet from `dev`, and stable packages to NuGet from version tags.

## Architecture

Six source projects, four test projects, one sample (each `src/` project ships a NuGet package, except the generator, packed inside the core one):

- **`src/Scrinium.Core`** (`Etherna.Scrinium.Core`) — The framework itself, host-agnostic. Main areas:
  - `DbContext.cs` / `IDbContext.cs` / `DbContextEngine.cs` / `IDbContextEngine.cs` — unit of work and its engine, related by composition (`IDbContext` does NOT extend `IDbContextEngine`). `DbContextEngine` owns the scope independent members built once at initialization (connections, schema registries, seeding cache, exclusive access locking with `RunWithExclusiveAccessAsync`, the lazily built `DbContextLock` and the application resource locks built by `GetResourceLock` — all denied on a read-only db context, whose lock collection belongs to the owner application), and is the type captured by the serialization pipeline; `DbContext` attaches to its engine (exposed as `IDbContext.Engine`) and owns the current unit of work state (`ChangedModelsList`, `SaveChangesAsync`, per-instance repositories, migration and resource lock facades, `ExecuteInTransactionAsync`). The internal facing members stay off the `IDbContext` application surface, implemented explicitly by `DbContext`: the generated proxies invoke the db context through `IProxyModelsDbContext` (change tracking signals and load reaction hooks, public because the proxies are emitted into the consumer assemblies), and the serializers and repositories through the internal `IInternalDbContext` extending it (model tracking and identity map bookkeeping); the application surface keeps only the identity map escape hatches `TryGetLoadedModel` and `UnregisterLoadedModel`. The engine has its own internal facing surface, `IInternalDbContextEngine` implemented explicitly by `DbContextEngine`: the in flight operations counter the guarded collections enter (a hard cast, so the wrappers never run on an engine without it; a mocked engine exposes it with Moq `As<>()`). The database name (`DbContextOptions.DbName`) is the path segment of the connection string, parsed with the driver `MongoUrl`: a connection string without it fails fast with a detailed `InvalidOperationException` that never reports the connection string, since `DbName` is a structured field of every log line and a connection string carries credentials in its user info section.
  - `Repositories/` — `Repository<TModel, TKey>` with typed access to collections; every collection access passes through `AccessToCollectionAsync`. The indexes defined on a collection (`GetDefinedIndexModelsAsync`, built and pruned by the migration index steps) are the custom ones of `RepositoryOptions.IndexBuilders`, taking the name rendered from their keys when declared without one, plus an automatic sparse index for each id path of the referenced documents: a reference id path opening a custom index — its first key, with an ascending or descending sort order — doesn't get its automatic index, since the custom one already serves the queries on that field, with the configuration chosen by the application. `EstimatedDocumentCountAsync` sizes a collection from its metadata, at constant cost. Paged queries (`QueryPaginatedElementsAsync`, and the `Paginate`/`PaginateDescending` extensions behind it) validate their paging arguments before querying: a negative page, a take below 1, and a page whose skipped elements amount exceeds `int.MaxValue` all fail with a detailed `ArgumentOutOfRangeException` — the elements to skip are the product of the paging values, so an unreachable page reports as an argument error instead of reaching the server as an invalid `$skip`. `CountDocumentsBySchemaIdAsync` groups the collection documents by the model map schema id they carry, resolving it from the current schema id element name or from the deprecated one (documents with no schema id element count aside): schema ids not registered on the db context report too, so the result tells what a migration would have to convert. The grouping can't use an index (the server scans the collection even when the schema id element is indexed), so its cost is linear in the collection size: it belongs to on demand diagnostics, never to a polled or startup path. `FindMissingOriginReferencesAsync` scans the collection references for missing origin documents: for each reference id element path — from every registered schema of the concrete model types the collection can store, since a reference written by a since deprecated schema still points to its origin document — the distinct referenced ids are read server side (one aggregation flattening the array levels with unwinds and grouping on the id path, disk use allowed) and verified as stored, with chunked `$in` reads against the origin collections resolved from the reference serializers, cross db context sources included. Null references address no origin document and stay out; the paths the scan can't verify (an unknown document key or a fixed array position in the path, or an origin repository not resolvable on the current scope) report apart as unverifiable, and their references stay untouched by any removal. The report lists the missing origin ids up to a cap per path (`MissingOriginReferencesPathReport.MaxTrackedMissingOriginIds`) keeping the full counts, and counts the documents referencing the tracked ids — a lower bound when the cap drops some. `RemoveMissingOriginReferencesAsync` runs the same scan, never a previous find's output, and repairs each missing origin reference while streaming: a reference hosted as an array item is pulled out of its array, any other one is set to null (addressed through an array filter on the missing origin id when arrays are in its path), with every update filtered on the id itself so a reference concurrently rewritten to another document stays untouched; a read-only repository denies the removal fail-fast. Both scans belong to on demand diagnostics too, reading every referenced id of the collection. `CountDeprecatedSchemaIdDocumentsAsync` counts the documents carrying their model map schema id under the deprecated element name Scrinium still recognizes (`ModelMapSchema.DeprecatedIdElementName`), matched at their root: a document whose root carries the current element name was written whole by a version writing it, its sub-documents included, so the root tells the whole document — while the opposite shape, a root written with the current name over sub-documents written with the deprecated one, doesn't happen, a whole document write being what moves a root to the current name. The count matches on the stored elements, so it scans the collection and belongs to on demand diagnostics like the other repairs. `MigrateDeprecatedSchemaIdDocumentsAsync` repairs them, and repairs them by migration: renaming the root element wouldn't be enough, since a document nests a schema id as deep as its sub-documents and reference summaries go, so each counted document is deserialized and written back whole with its current active schema, which stamps the current element name at every level. It runs a `DocumentMigration` restricted to them with its `DocumentsFilter`, inheriting its behavior — failing documents skipped and reported, transient models evicted at intervals, referencing documents not updated — and a read-only repository denies it fail-fast. The upsert helpers (`UpsertAsync`, with its `UpsertAddToSetAsync`/`UpsertIncrementAsync`/`UpsertSetFieldAsync` shorthands) compose the `$setOnInsert` instructions from the elements of the serialized `onInsertModel`, excluding its id and the updated fields: an element is set whole, unless an updated field navigates into it — two operators writing the same branch make the server refuse the update as a conflict — in which case it splits into its sub elements, recursively, down to the updated field, which stays out alone, so the document an upsert creates keeps the siblings of a nested updated field: the keys of a dictionary in document representation beside the incremented counter key, the nested updated field an application legitimately addresses (an embedded value object is immutable, so it is replaced whole, never written inside); a value that can't split — an array, for instance, since a path inside it isn't an independent branch — has no composition at all: it can't stay whole beside the updated field, and without it the update alone would insert a nested document in its place, a value of another type that the model can't even read back, so the upsert fails with a detailed `InvalidOperationException`. The element names composing an update field name — the top level ones, and the ones of a split sub document — are taken verbatim, so they have to be usable as update path segments: a name containing a `.` addresses a nested field (the same model an insert writes with a literal dotted element, an upsert would write nested, silently), and an empty name has no update path addressing it, so both fail with a detailed `InvalidOperationException`; a name of either shape inside a sub document set whole is stored as a value, like an insert stores it, and passes (a `$` prefixed name isn't read as an operator inside `$setOnInsert`, and stores like an insert stores it too).
  - `Serialization/` — model maps and versioned schemas (`MapRegistry`, `ModelMap`, member maps), discriminator registry, serializers and modifiers. Model map schema ids must be unique across the whole db context, not only inside their model map, with the `fallback` id reserved to fallback schemas: violations fail fast at engine build with a detailed `ScriniumDuplicateSchemaIdException`. Document discriminators (the `_t` element value, defaulting to the simple type name, overridable with `BsonClassMap.SetDiscriminator`) must identify one single model type: a discriminator shared by different model types resolves multiple candidates at read, and the read fails as ambiguous as soon as the nominal type of the deserializing member is satisfied by more than one of them (any `object` shaped member is), so a shared discriminator fails fast at engine build with a detailed `ScriniumDuplicateDiscriminatorException` — unless every model type sharing it is abstract, since an abstract type is never the concrete type of a serialized instance and its discriminator is never written nor looked up. Documents carry their model map schema id in the `_s` element (`ModelMapSchema.IdElementName`, a constant beside the deprecated `ModelMapSchema.DeprecatedIdElementName` and the reserved `ModelMapSchema.FallbackId`): reads also recognize the deprecated `_m` name, so documents written with it keep deserializing, and migrate to the current name with their next whole document write (a member level save guards on the current name only, falling back to the migrating replace), or in bulk with `IRepository.MigrateDeprecatedSchemaIdDocumentsAsync`, from the admin dashboard. The schema id is document content, and selects which registered schema — hence which member set, and which post-load fix function — deserializes the document: a collection also written by another application drives that selection, inside the registered configuration. An id matching no registered schema, a missing one included, deserializes with the configured fallback serializer or schema, the declared handling of unknown documents; without one it degrades to the default handling — the active schema at root, the reference id alone on references — reported by a warning deduplicated per model type and schema id, up to the `MaxWarnedUnrecognizedSchemaIds` distinct ids a serializer remembers: that degradation is otherwise invisible, and the reference one turns every member access into a lazy load of the origin document. The mapped id member of every entity model map (reference configurations included) must be the implicit `IEntityModel<TKey>.Id` implementation — the typed id contract addressed by repositories, identity map and references, and the persisted identity, must be the same member: violations fail fast at engine build with a detailed `ScriniumInvalidIdMemberException`. An entity id is also always a value, never a composite: repositories, identity map and references address a document by an atomic key, and a document valued id is the only shape MongoDB reads as an operator expression instead of a value (a filter value document whose first element name starts with `$`), so a caller sending the id `{"$ne": null}` would match, delete or overwrite an arbitrary document. The same exception rejects at engine build every id member whose serializer declares a document or an array representation (class mapped composite ids, dictionaries, `BsonDocument` and `BsonValue` members). The shapes a serializer doesn't declare — a custom serializer emitting a document — are refused with a `FormatException` when they render: by the repository id filters (`EntityIdEqFilterDefinition`), by the dependencies update task filters on an id member (`MemberMapEqFilterDefinition`), and by the create write, that builds the id filter of the inserting model so a document Scrinium couldn't address again is never persisted; `TryFindOneAsync` reads the refused id like any other unparsable key, an id matching nothing. A composite id serializes into a value (a string, for instance), keeping its components as mapped members to query them. Member equalities compare explicitly: `MemberMapEqFilterDefinition` renders `{element: {$eq: value}}`, and inside an `$eq` MongoDB compares every value literally, so a member serializer deriving element names from the serialized value can't turn an equality into another query whatever value it receives (the implicit `{element: value}` form reads instead a value document whose first element name starts with `$` as an operator expression). An id also commits to its type, which an `object` typed id doesn't: the driver object serializer writes the values with a BSON type equivalent as plain values and discriminates any other one into a document, and a value reads back as the type of its BSON type (an enum id writes `1` and reads back an `Int32`), while the typed entity id contract, the identity map keys and the references resolution all rely on the id value type; the same exception rejects an id member typed `object`, unless the application maps its own serializer for `object`, declaring how its values serialize and deserialize. The serializer explorations terminate on self referential serializers (the driver `BsonValue` serializer reports itself as its own array item serializer), so non id `BsonValue` members map normally, and the element path of such a member closes on the cycle instead of growing without end. Entity ids can use types mapped with a custom serializer map: the map claims its serializer registry slot at registration, so serializer lookups running before maps registration completes (e.g. the driver id generator convention resolving the id member serializer at automap) resolve the custom serializer also for types otherwise served by the driver providers (like Guid, whose driver default serializer fails at use with its unspecified representation); a lookup resolving earlier caches the `MappedSerializerAdapter` fabricated by the serialization provider, delegating every operation to the mapped serializer and kept as the registered serializer at map registry freeze. `typeof(object)` never gets a model map (base model map linking stops before it), keeping the driver `ObjectSerializer` registered for `object`: object shaped members (plain `object`, `Dictionary<string, object>` bags, and interface typed members through the driver discriminated interface serializer) are guarded by its allowed types — a mapped model type selected by a document discriminator is denied, reading and writing; an application hosting its own types into object shaped members registers an `ObjectSerializer` with an explicit allow list through a custom serializer map. Reference serializer configurations are separate id spaces (their summary schema ids can mirror the root ones) and stay out of the check. Root model map schemas can declare a post-load fix function (`fixDeserializedModelFunc`), invoked on the root deserialization path only: reference schema configurations build through their own `IReferenceModelMapBuilder` surface, which doesn't accept the hook — a model fix belongs to the origin document's root schemas, and reference members lazy load from the fixed origin document. Reference documents with an unrecognized or missing schema id deserialize through the configured fallback serializer or schema when present, or by default reading only the reference id (any other member lazy loads from the origin document). A model deserialized from a reference document is a summary whose loaded member names derive from the document elements, mapped through the member maps of the schema deserializing it — independent of setter observability, so members set through private or non virtual setters report as loaded too, while a member missing from the document (including one assigned by a specified serializer default value) lazy loads at its first get; only a custom fallback serializer, deserializing without a schema, keeps the proxy-observed setted members as source. Dates are UTC instants persisted as BSON DateTime: every persisted `DateTimeOffset` member of the internal models (migration operation and logs) sets `DateTimeOffsetSerializer(BsonType.DateTime)` on its member map (offset and sub-millisecond precision discarded; the driver serializer reads any other driver representation too, keeping its original offset). Model timestamps are `DateTimeOffset` values generated with `UtcNow`. There is no registry-wide default: application models configure their own `DateTimeOffset` members the same way (unconfigured members persist with the driver default, the `{DateTime, Ticks, Offset}` document).
  - `ProxyModels/` — source generated model proxies (emitted at compile time by `src/Scrinium.Core.Generators`, one sealed `XProxy` subclass per concrete entity model, registered with assembly level `GeneratedProxyModelAttribute`) enabling lazy loading of referenced models (`IReferenceable`). Change tracking is snapshot based and lives on the db context, not on a proxy interface: a proxy only flags itself a change candidate on mutation (through the generated member overrides), so non proxy models are trackable too. Proxy types have no registered model maps: serializers purge a proxy instance to its concrete model type at lookup and serialize it through that type's class map as the nominal type (`SerializeAsNominalType`). Implicit lazy loads are synchronous over the db call and honor `DbContextOptions.ImplicitLazyLoad`: load logging a deduplicated warning (default), load silently, or deny throwing `ScriniumLazyLoadingException`. Performance sensitive code preloads explicitly with `IDbContext.LoadValuesAsync` (per instance, or batched per source repository with one `$in` query per bounded chunk of ids — members are a no-op precondition, any load is of the whole document, merged in place through the identity map) and inspects with `IsMemberLoaded`. A load finding no origin document, because the referred document doesn't exist anymore on the origin collection, reacts as the reference that deserialized the summary declares (`ReferenceSerializerConfiguration.MissingOriginDocument`): by default it logs a warning deduplicated per model type and source repository and gives up the summary state, with the not loaded members at their default values — a domain delete legitimately opens this state until its background propagation repairs the references, so an exception can't be the default reaction inside that window — or it tolerates silently, or denies the load with a detailed `ScriniumMissingOriginDocumentException` (`Throw`), the strict opt-in of the references that must not tolerate the inconsistency. The counterpart at the source is the origin delete policy of the reference (`ReferenceSerializerConfiguration.OriginDelete`): a model deleted through its repository propagates in background to the documents referencing it — the reference removed (the default, so a domain delete never leaves its own references dangling), or the referencing document deleted — with `KeepReference` as explicit opt-out, and the propagation semantics on the tasks bullet below. Implicit and explicit loads react the same way — a model still summary after its preload found no document — and every summary is born from a reference, carrying the declared mode with it: one document materializes one single instance whatever the references reaching it, so an instance reached also by a stricter reference keeps the stricter mode. A document can change its concrete type over time, keeping its id: a full load finding a type not matching the loaded instance registers the fresh instance as the loaded one (returned by that and every next load) and invalidates the outdated instance, whose runtime type can't upgrade — any application interaction with it throws a detailed `ScriniumOutdatedModelTypeException` (inspect with `IDbContext.IsOutdatedModel`), while the library internals keep reading it under change tracking suppression. The id member is never proxied (resolved as the implementation of the typed entity model interface id, with the `Id` name as convention fallback; at runtime, as the mapped id member of the active schema): identity is definitionally present on any instance — never joining the summary member names, always reported loaded, skipped by the generated merges — and stays readable also on an outdated instance, where it drives the reload.
  - `Migration/` — `DocumentMigration` scripts between document schemas. A migration scans the whole collection, unless its `DocumentsFilter` restricts it to the documents to rewrite, selected on their stored content since a migration also addresses documents the current maps don't shape anymore. Documents scan raw, deserializing each one apart (a typed cursor can't survive a document failing deserialization): a document failing deserialization or processing is skipped, keeping its current content, and reported into the result errors with its id and error (detail capped at `DocumentMigration.MaxTrackedDocumentErrors`, with the full count aside), while every other document migrates — `MigrationResult.MigratedDocuments` counts the documents processed without errors, `ProcessedDocuments` the whole scan (migrated plus failing ones). The documents processed between two evictions run inside a transient models scope of the db context, evicting at its end everything their flows loaded or tracked (the migration operation bookkeeping, tracked before the scan, stays tracked): the scan memory stays bounded to an eviction interval on any collection size. The interval is `DbContextOptions.MigrationEvictEveryTotDocuments` (100 documents by default), independent from the progress report interval `DbContextOptions.MigrationCallbackEveryTotDocuments` (500): one tunes the retained memory, the other the writes on the operation document. The scan cursor lives as long as the session the driver keeps alive for it, since the server side cursor idle timeout only reaps the cursors without a session. A run can ask to abort at its first failing document instead (`stopAtFirstError`), and can execute as a **dry run**: the processor executes under an ambient `DryRunHandler` (collection writes execute only their client side — definition rendering and document serialization — returning simulated results without touching the server; writes without a client side half — index management, aggregate to collection (a pipeline ending in `$out` or `$merge` included) and map reduce with an output collection — throw).
  - `Tasks/` — background tasks invoked through `ITaskRunner` (`UpdateDocDependenciesTask` propagates updated summaries to referencing documents, reserializing each sub-document with its reference member serializer — reference schema shape, schema id, and discriminator of the current referenced model type — and writing server side with one `UpdateMany` per hosting repository and reference id path, without per document round trips nor content read back; the referenced model reads back through the source repository of the involved references, resolved on the task db context like a lazy load would — the child db context attached to the scope, for a task refreshing the parent documents of a changed child model; read-only repositories are skipped since their documents belong to another application; a reference id path containing an unknown document key (a dictionary in document representation) can't render an update filter — querying unknown document keys is unsupported server side (SERVER-267) — so the task skips it and its summaries stay stale: the engine build reports every such element path, one per hosting model type and path, with the reaction declared by `DbContextOptions.NotPropagatedReferences` — a warning per path (the default), silent tolerance, or a detailed `ScriniumNotPropagatedReferenceException` denying the build — making the limitation a conscious configuration choice; `DeleteDocDependenciesTask` propagates a domain delete to the referencing documents, applying per reference id path the origin delete policy of the reference (`ReferenceSerializerConfiguration.OriginDelete`, removing the reference by default, with `KeepReference` as explicit opt-out): the reference removal repairs server side with one update per hosting repository and path — array items pulled out of their arrays, single members set to null, through the shared `ReferenceRemovalShape` also serving the missing origin references removal — while references declaring the referencing document delete load the referencing models batch by batch inside a transient models scope and delete them with a domain delete, chaining their own policies (termination is guaranteed: every step only deletes still existing documents, so mutual references stop on the already deleted ones); schemas sharing an element path apply the most invasive policy of the group, paths with an unknown document key or a fixed array position stay untouched (found by the missing origin references scan), read-only repositories are skipped since their documents belong to another application, and only domain deletes propagate — raw bulk deletes and deletes by other applications keep the references; `MigrateDbContextTask` runs a db context migration under exclusive access — except dry run operations, running without it since they persist nothing, while the db context lock claimed with their operation applies like for any other migration).
  - `Utility/` — `DbMaintainer` (enqueues dependency updates on model changes, only when the changed members resolve at least one reference id member map: without involved reference members the task would have nothing to propagate, so the enqueue is skipped; enqueues the delete propagation on domain deletes with `OnDeletedModel`, only for the reference id member maps whose serializer declares an origin delete policy other than keeping the reference, can host the deleted model type, and resolves its source on the deleting repository; both propagations cross the engines of the application: the member maps select with the same filters also on the map registries of the parent db context engines — resolved by `IParentEnginesProvider`, implemented by the DI layer — keeping only the references sourced on the changed repository, recognized by the source db context type declared with the typed factory (selectors of untyped or implicit sources expect their own db context instance, so they never source another engine), with one task per involved parent engine and read-only parent db contexts skipped, their documents belonging to another application; each task payload carries the db context type and name of the changed repository, re-verified together at execution, since repository names are unique per db context only), `DbMigrationManager` (drives db context migrations and their `DbMigrationOperation` log: any failure, unhandled exceptions included, marks the operation failed — never left on running status, which would misreport a migration in progress — logs at error level, and throws only when the caller asks for errors; an execution unable to resume the db context lock claimed with its operation (taken over by another owner, or released) closes it cancelled without migrating, and a start failing after its claim (the orphans sweep, or the task enqueue) releases the claim and deletes its operation before rethrowing; failing documents don't abort an operation, that completes its scan and closes failed with their detail, unless started with `TryStartMigrationAsync(stopAtFirstError: true)` (`DbMigrationOperation.IsStopAtFirstErrorEnabled`); documents migration progress reports through one rolling executing log per collection with its running counter (`DbMigrationOperation.AddDocumentMigrationLog`), replaced at the end by the ended log, so the operation document stays bounded regardless of collection sizes and its final status write always fits the document size limit; a dry run operation, `DbMigrationOperation.IsDryRun` started with `TryStartMigrationAsync(dryRun: true)`, skips the index steps and simulates the document migrations, persisting only the operation log with the failing documents detail), `ResourceLock` (server side lease lock over a resource, one raw document per lock in the `_db_lock` collection — `DbContextOptions.DbLockCollectionName`, accessed out of the engine access limitations: an atomic upsert claim elects a single owner across every application instance connected to the database (applications configuring different lock collection names for the same database don't exclude each other), the running flow keeps the lease renewed in background through `IResourceLockLease`, and an expired lease of a dead owner — like a document without a date expiration — is taken over by the next claim, without manual repair; the first claim of an engine also ensures a TTL index on the collection, garbage collecting the documents abandoned by dead owners `ResourceLock.AbandonedDocumentRetention` after their expiration, with a creation failure logged as a warning without denying the claim. The engine binds one lock to its own identifier (`IDbContextEngine.DbContextLock`, coordinating seeding and migrations once per db context), and applications bind their own with `IDbContextEngine.GetResourceLock(resourceNamespace, resourceId)` — the lock of an arbitrary resource id inside a namespace the application chooses, with the one call facades on `IDbContext` (`TryAcquireResourceLockAsync` and `IsResourceLockedAsync`) as the client surface — identified by the plain string id `namespace/resourceId` (ids are values, never documents, like every Scrinium id): the namespace is denied to contain the `/` separator, so locks of different kinds never collide, while the resource id, being the last segment, stays free; a db context identifier carrying `/` could alias an application lock id, so keep identifiers out of that shape. Locks are exclusive or shared (`TryAcquireAsync(ResourceLockMode)`, exclusive by default): an exclusive acquisition — claim and renewed lease in one atomic command — is denied by any live lease, while a shared one admits any number of holders, each with its own lease entry in the `SharedLeases` array and its own renewals, denied only by a live exclusive lease; the top level expiration tracks the furthest lease, kept there by the shared acquisitions and renewals, so exclusive claims stay denied while any holder lives, a dead holder expires alone instead of leaking a holders count, expired entries are dropped by the next acquisition or release, and the release of the last holder deletes the document (a shared acquisition colliding on the insert with concurrent claims retries bounded times, reading whether a live exclusive lease is the true denial). The lease duration is chosen per claim (`TryClaimAsync(ownerId, leaseDuration)` and the acquisitions, `ResourceLock.DefaultLeaseDuration` of 10 minutes when unspecified, and at least `ResourceLock.MinLeaseDuration`, since renewals run at a fifth of it): it is not the duration of the work, kept renewed while it runs, but how long the lock stays claimed when its owner dies, and how long a claim survives before someone resumes it. The chosen duration travels in the lease document (`LeaseDurationTicks`), because claimer and renewer are frequently different processes — the web process claiming a migration start, and the background worker executing it: a resume reads it back to renew on it, falling back to the default when the document carries none (the claim and resume split is exclusive only; an acquired lease lives and dies with its process). `IResourceLockLease.LeaseLostToken` cancels the work under it when a renewal finds the lock taken over, or renewals keep failing for the whole lease duration. Each resume stamps a fresh lease id, and renewals and releases match the lease identity — owner and lease id together, or the lease id of a shared entry: two executions of the same operation fence each other, instead of the loser releasing the lease of the winner. A claim never resumed into a lease is released with `TryReleaseAsync`, and `IsLockedAsync` reports a live lease, exclusive or shared, telling an operation in progress from one orphaned by a dead owner), `ExclusiveAccessHandler` + `LimitedAccessMongoCollection` (deny read/write access to collections while another context holds exclusive access, counting every guarded operation in flight until its forwarded call completes so the exclusive window drains the admitted ones before starting), `DryRunHandler` (marks a flow whose collection writes are simulated; `DbMaintainer` skips the dependencies propagation under it), `MapsRegistrationHandler` (marks the flow registering the maps of an engine: the conventions Scrinium registers on the driver global convention registry apply only inside it). Change tracking is snapshot based: at load and create the db context captures a per instance model document (the serialized members); a proxy flags itself a change candidate on mutation (through the generated member overrides), and non proxy tracked models (created or replaced instances) are always diffed. `SaveChangesAsync` diffs each model against its model document to compute the changed members, so a diff with no change saves nothing. Loaded models are deduplicated per db context instance (identity map, EF-like): one document materializes one instance inside a scope, references to already loaded documents return the existing instance, a full load upgrades a loaded summary in place (`MergeFullModel`), and a full load finding the document with another type of its hierarchy replaces the loaded instance with the fresh one, invalidating the outdated instance (`ReplaceOutdatedLoadedModel`); deletes and upsert old snapshots evict from the map. The no cache serializer modifier disables both change registration and deduplication. A transient models scope (`IDbContext.StartTransientModelsScope`) evicts at its dispose every model loaded or tracked inside it, keeping the ones entered before with their updates: massive scans (like the document migration one) run each element inside such a scope, so each element still loads and saves normally while the scope memory stays bounded.
- **Origin repositories** (hierarchically dependent model types): a db context can declare multiple repositories over the same model type, or over types of the same inheritance chain. The association "reference member → origin repository" is static, configured on the reference serializer with the typed factory (`ReferenceSerializer.Create(engine, config, sourceRepository: (IMyDbContext dbContext) => dbContext.MyRepo)` — generic arguments fully inferred from the selector, source compatibility with the reference model and key types checked at compile time; the declared db context type resolves once inside the library — the current scope db context when it implements it, or the child db context attached to the scope otherwise — failing detailed when neither applies); the untyped constructor parameter (`sourceRepository: dbContext => ((IMyDbContext)dbContext).MyRepo`) stays as escape hatch for the one declaration invariance can't express — a base-typed repository sourcing derived-typed references — and stays same-db-context only. Selectors are invoked per scope, since repositories are per-scope instances. Reference serializers without `sourceRepository` RESOLVE it at engine build to the single db context repository property compatible by model and key type (`BuildEngine` reads the repository property values off its own builder instance): ambiguity fails fast at startup with a detailed `ScriniumAmbiguousRepositoryException`, and references without any compatible repository fail fast with a detailed `InvalidOperationException` pointing to the cross db context declaration — every reference of a built engine binds a source repository, so the identity map lookups are always by repository. Declared source repositories are validated at engine build (`BuildEngine` invokes the selectors on its own builder instance): a repository not hosting the reference model type, or with a different key type, fails fast with a detailed `ScriniumInvalidEntityTypeException`. **Cross db context references**: a typed declaration on a db context type not implemented by the builder declares its source on another db context — the declared type must be reachable through exactly one child db context type of the options (`DbContextOptions.ParentFor`), validated at engine build with a detailed `InvalidOperationException` when missing or ambiguous (the selector can't run on the builder, so the compile time check of the typed factory is the compatibility guarantee). At deserialization the source resolves on the child db context instance attached to the scope, and the referenced model homes there entirely: identity map, model document and change tracking live on the child db context (one instance per document also across the parent and child contexts of the scope), lazy loads run through the child repository, mutations persist with the child save — cascaded by the parent `SaveChangesAsync` — and new referred models (null id) auto create into the child repository (out of the parent implicit save transaction, being another engine). The dependencies update and delete propagations cross the engines of the same application: a child model changed or domain deleted through a db context of the application refreshes or repairs the summaries denormalized into the documents of its parent db contexts, with one background task per involved parent engine. The boundary is the application: documents of applications not hosting the parent db context keep their summaries, so denormalize cross db context summaries only when every writer of the child collections also hosts the referencing db contexts — otherwise keep them id-only, unless staleness is acceptable. Binding is member-exact: reference serializers always push their resolved source around the inner deserialization, paired with the db context owning it (a null repository shadows the outer operation one when unresolvable), root documents bind the repository reading them (carried by `DbExecutionContextHandler.Repository`), and proxy models bind the current operation repository verbatim at creation — no runtime type deduction. `IReferenceable.SourceRepository` is NOT nullable: every proxy materializes inside an operation addressing a collection, so `ProxyGenerator.CreateInstance` binds its source repository or fails fast with a detailed `InvalidOperationException` — an instance unable to save or lazy load is a broken flow, not a supported state. The db context binding stays nullable instead, since the no cache serializer modifier deliberately clears it to keep read only massive scans out of the unit of work. Entity models serialized as full embedded documents (instead of references) are unsupported: the maps freeze detects every member serializing an entity model type through a class map, reference configurations included, and fails fast with a detailed `ScriniumEmbeddedEntityModelException` — at most, a reference can denormalize every member, staying a reference with its own source. A custom serializer, set on the member or mapped for the type, never enters the document serialization pipeline: the opt-out for value-object-like models. Saves, lazy loads and the identity map key on the bound source repository. The dependencies update task fans out to every repository handling a referencing root type.
- **Changes save semantics** (`SaveChangesAsync` → `Repository.SaveChangesAsync`): each changed model is persisted with a single atomic `FindOneAndUpdateAsync` setting only its changed members (`$set`, or `$unset` for members excluded by their serialization options), guarded by the active model map schema id in the update filter — the check is in the filter to be atomic with the update, because setting members serialized with the active schema into a document shaped by an older schema would mix schemas into a broken document. Documents on a not active schema (or with changed members not mapped by the active schema) fall back to a whole document replace that migrates them; repositories can opt into whole document replacement with `RepositoryOptions.SaveWithDocumentReplace`, and direct `ReplaceAsync` calls always replace the whole document (the explicit atomic replace escape hatch). The returned updated document refreshes the saved model in place (`MergeFullModel`): after a save, members not changed locally can update with concurrent changes from other scopes — the save is the synchronization point of the unit of work — and a saved summary upgrades to a full model. Conflict granularity is the member: concurrent changes to disjoint members all survive; changes to the same member (arrays included, e.g. concurrent adds to the same collection member) stay last writer wins — full protection would need optimistic concurrency (proposal tracked separately). **New referred models auto create**: an entity model referred with a null id is a new model, and every repository write serializing references (create, member level save, whole document replace) creates it into its source repository first. A discovery pass serializes the persisting model (the changed members for a member level save) under an ambient collector reported by the reference serializers; the ids of the persisting and discovered models are assigned upfront through the mapped id generator, so references between new models serialize complete in any creation order, cycles included; each discovered model then inserts with its change tracking started, without flushing the unit of work (inside a member level save, the inserts enlist in the implicit save transaction). A new referred model whose reference member doesn't resolve a source repository, or whose id member has no id generator, fails the write with a detailed `InvalidOperationException`; and serializing a reference to a model without id outside a discovery pass (upserts and raw collection writes, which skip the auto creation) throws too — a null id reference deserializes to null, so it never persists silently. Reference documents without id already persisted keep deserializing to null.
  - `Domain/Models/` — internal operation log entities persisted in the `_db_ops` collection (`SeedOperation`, `DbMigrationOperation` with its `DbMigrationOpAgg/` logs).
- **`src/Scrinium.Core.Generators`** (`Etherna.Scrinium.Core.Generators`) — Roslyn incremental source generator emitting the proxy model types (netstandard2.0, the required target for compiler extensions). Packed inside the `Etherna.Scrinium.Core` package under `analyzers/dotnet/cs`, so every consumer referencing the package generates proxies for its own entity models with no setup; in-repo projects defining models (core, tests, sample) reference it explicitly with `OutputItemType="Analyzer"`, since analyzers don't flow through project references. Inspect the emitted sources with `-p:EmitCompilerGeneratedFiles=true` (under `obj/GeneratedFiles`).
- **`src/Scrinium.AspNetCore`** (`Etherna.Scrinium.AspNetCore`) — DI integration: `AddScrinium` configuration builder, keyed singleton `IDbContextEngine` + scoped `DbContext` registration, `DbDependencies`, execution context wiring. Each `AddDbContext` records its `DbContextRegistration` (service and implementation types, options), and `ParentEnginesProvider` resolves over them the parent engines of a db context type — the engines whose options declare its registration among their children, built on demand from the keyed services — serving the cross engine dependencies propagation.
- **`src/Scrinium.AspNetCore.UI`** (`Etherna.Scrinium.AspNetCore.UI`) — Admin dashboard as a Razor Pages area (`Areas/Scrinium/Pages/Index*`), mapped on a configurable `DashboardOptions.BasePath` and guarded by the `DashboardOptions.AuthFilters` filters (`IDashboardAuthFilter`), snapshot when the authorization policy is built: an access is granted when every configured filter allows it and is denied by the first one denying it, so a list without filters leaves the dashboard unrestricted. The default filter (`LocalRequestsOnlyAuthFilter`) authorizes only requests coming directly from the application host — a loopback or local-address peer, with IPv4 mapped IPv6 forms recognized — and denies any request carrying forwarding headers (`Forwarded`, `X-Forwarded-*`, `X-Original-*`, `X-Real-IP`): behind a proxy the connection address identifies the proxy, not the client, so an address based filter can't authorize there and proxied deployments must configure a filter validating an authenticated principal. Static assets are self-contained in `wwwroot/` (no external client libraries); the status/start endpoints are page handlers polled by `wwwroot/js/scriniumDash.js`. The layout links the dashboard tab icon explicitly, on the `_content/Etherna.Scrinium.AspNetCore.UI/` base path of the package assets, since a browser requests `/favicon.ico` by convention only at the web root: `favicon.svg` follows the browser color scheme with a `prefers-color-scheme` media query (the ink symbol on light, the papyrus one on dark), and `favicon.ico` (16, 32 and 48 px) is its fallback, declared with a fixed size so the browsers supporting SVG icons prefer the SVG. The header brand carries the same symbol inline (`.brand-mark`), filled with the color of the brand text, so it renders light on the dark header whatever the color scheme, without an asset request. The state changing POST handlers keep the default Razor Pages antiforgery validation: the page renders the token, and the script sends it back as the `RequestVerificationToken` header of every POST, while the GET status handlers stay token free. Everything renders as one card per db context: migrations can start also as dry run, or asked to stop at their first failing document, and operations render their flags and failing documents. An "Advanced" collapsible section, collapsed by default, carries the db context lock lease duration of the start, in minutes (defaulted to `ResourceLock.DefaultLeaseDuration`): the start handler validates it server side, rejecting an absent, non positive or over `IndexModel.MaxLockLeaseDurationMinutes` value with an error the page renders in its feedback paragraph, without starting anything. A migration reports as running (and locks the start controls) only while an open operation is paired with a live db context lock lease (`IResourceLock.IsLockedAsync`, one more query on the status path): an operation orphaned by a dead instance keeps the controls enabled, since a start is what closes it. Each card also carries a model schemas section, listing per collection the schema ids its documents can carry — the active and secondary schemas of the concrete model types assignable to the repository model type, since a document carries the active schema id of its own concrete type (fallback schemas stay out, their reserved id is never written). The page renders this from the maps alone, without touching the database; expanding the section sizes each collection with the estimated count (constant cost), and the linear cost schema ids count runs only on the explicit per collection request, adding a row for each schema id found on documents but not registered, and one for documents with no schema id: a non zero count outside the active schemas is what a migration has to convert. Each card also carries a document structures section, rendering per collection the shape of its documents, one per registered model map schema of the concrete model types stored there (fallback schemas stay out, read only shapes never written): the elements the schema writes, in the order it writes them, with the schema id element carrying the id of that schema, and each sub-document expanded into its own shapes. A sub-document is written by the concrete type its member receives, whatever the declared one, so its shapes are the registered schemas of every concrete model type assignable to it — a member declaring an abstract type renders the shapes of its derived types, and the enumeration is complete because every serialized model type has a model map, a document of a type without one failing its write. A reference takes its shapes from the model maps of its serializer configuration instead, which don't live in the registry. The exploration closes on the schemas already open on its own path: a model graph cycle (a model nesting itself through the shapes of its derived types, or a summary denormalizing a reference to its own model) closes there reporting a `cycle`, and naming the shape it repeats, instead of nesting shapes until the stack ends. The elements carrying the summary of a referenced document report as `reference` (an entity model is never embedded, so a sub-document shaped by an entity schema is a summary) and expand with the members denormalized into them, the ones whose update rewrites that summary in every document carrying it: reading the whole structure tells how a document looks in the database, denormalized data included. A reference whose model types have no repository on the db context reports also as `external`, since its documents are saved on the db context owning them and their changes refresh the summary only when applied by an application hosting both db contexts, and one the update filter can't address (an unknown document key in its path) reports as not propagated. The extra elements bag stays out of the shapes, carrying no element of its own, and the containers of an element render one symbol each (`[]` an array, `{}` a dictionary written with its keys as element names). This renders from the maps alone too, exploring the member serializers like the member maps do, with self referential serializers (the driver `BsonValue` one, its own array item serializer) closing the exploration instead of nesting containers without end. Each card also carries a missing origin references section, finding per collection the references whose origin document doesn't exist anymore: the scan runs only on the explicit per collection request (it reads every referenced id of the collection, through the repository find), rendering per reference element path the missing origin ids — listing capped by the repository, counts complete — with the referencing documents count, and listing apart the paths the scan can't verify; on writable repositories a removal control repairs the found references through the repository removal, confirmed by the operator and rescanned right after, while the removal handler rejects read-only repositories also when the request doesn't come from the page. Each card also carries a deprecated schema id elements section: it counts per collection the documents still carrying their schema id under a previous element name, on the explicit per collection request (the count scans the collection, through the repository count), and on writable repositories a migration control rewrites them whole through the repository migration, confirmed by the operator and recounted right after, rendering the migrated documents and the failing ones with the errors that skipped them. The migration handler rejects read-only repositories also when the request doesn't come from the page, and reports the collection unavailable when an exclusive access failed the scan, which reaches it as the exception of a failed migration result, since a migration reports its failures instead of throwing. What each section reports is explained by an info tip beside its lead line, an icon opening the text on hover or focus (kept clipped in the document while closed, so the assistive technologies read it whatever the pointer does), and the tags of the structures explain themselves with their title: the page carries a lead line per section, not a paragraph. Read-only db contexts render without migration controls, schemas and structures sections included (the schema ids count is a read, and the structures render from the maps). `DashboardOptions.BasePath` replaces the area name as first route segment of every dashboard page, and is normalized at registration (leading, trailing and repeated `/` name the same path, and would otherwise render routes carrying an empty segment); left empty it mounts the dashboard on the application root — what an application dedicated to it wants, and a collision with the pages of any other. `DashboardOptions.AppPath` is the target of the layout back link (null hides it), validated at registration too: a relative path or an absolute `http`/`https` URL, with any other scheme refused by a detailed `ArgumentException` — the value renders as the href of the link, so a `javascript:` target would run in the dashboard origin; the scheme reads like a browser reads it (the name before the first `:` of the space trimmed value), and control characters are refused everywhere, since browsers discard them when parsing an href, so they could disguise a scheme. Every dashboard response denies storing and content type sniffing (`Cache-Control: no-store`, `X-Content-Type-Options: nosniff`), and the page also denies framing and any foreign content source (`X-Frame-Options: DENY` and a `default-src 'none'` content security policy, affordable because the page is self contained). Throttling the dashboard handlers belongs to the hosting application, which owns the request pipeline: a status poll queries every db context, and a migration start, dry run included, scans every migrated collection.
- **`src/Scrinium.Hangfire`** (`Etherna.Scrinium.Hangfire`, root namespace `Etherna.Scrinium.HF`) — `ITaskRunner` implementation scheduling Scrinium tasks on Hangfire.
- **`src/Scrinium`** (`Etherna.Scrinium`) — Meta package wiring the full stack (AspNetCore + Hangfire) with a single `AddScriniumWithHangfire` entry point.
- **`test/Scrinium.AspNetCore.UI.UnitTests`** — xUnit unit tests for the admin dashboard components (auth filters).
- **`test/Scrinium.Core.UnitTests`** — xUnit + Moq unit tests for the core.
- **`test/Scrinium.Core.Generators.UnitTests`** — xUnit tests running the generator in-memory with `CSharpGeneratorDriver`: selection rules, emitted shape, altered members computation, and the compile check of the generated sources.
- **`test/Scrinium.AspNetCore.UI.UnitTests`** — xUnit + Moq tests of the admin dashboard, hosted in-memory with `TestServer` over mocked db contexts: authorization of the dashboard area, antiforgery validation of the page handlers, hardening headers of the responses, base path and back link target validation, disclosure of the migration document errors, the document structures computed over a real map registry, the missing origin references handlers, and the deprecated schema id elements handlers.
- **`test/Scrinium.IntegrationTests`** — xUnit integration tests against a real MongoDB instance, pinning end-to-end behavior (change tracking, referenced models and lazy loading, execution scope isolation).
- **`samples/AspNetCoreSample`** — runnable demo app (not packed).

Key cross-cutting points:

- **The MongoDB driver is the Etherna fork** (`Etherna.MongoDB.Driver`, namespaces `Etherna.MongoDB.*`) — never reference the official `MongoDB.*` packages or namespaces. Fork >= 3.10.1 is required: proxy model serialization relies on `BsonClassMapSerializer` honoring `SerializeAsNominalType`, a fork-only behavior proposed upstream (CSHARP-3153) and pinned by the `DriverSerializationBehaviorTest` unit test — it must stay green on every driver bump.
- **Execution contexts**: `IExecutionContext` (`Etherna.Scrinium.Core.ExecContext` namespaces, vendored from the former standalone `ExecutionContext` package) provides ambient per-flow state (HTTP request or async-local scope). `DbExecutionContextHandler` associates the current db operation to the flow, always carrying the `IDbContextEngine` and, when the operation runs inside a scope, also the `IDbContext` (null for engine level work like schema registration); `ExclusiveAccessHandler` marks a flow as owner of the exclusive access of a given engine, which it carries; `MapsRegistrationHandler` marks the flow registering the maps of a db context engine. Handlers are `IDisposable` scopes registered in `context.Items`. One execution context serves one flow: the ambient state resolves by nesting inside a single flow, so parallel flows must each initialize their own async local context (startup seeding runs each db context seed inside its own). The handlers claim their `Items` bookkeeping lists atomically — every library access to the non thread safe dictionary locks on its instance (`ExecutionContextExtensions`) — so a dictionary shared by parallel flows can't corrupt, but the resolution semantics stay per flow.
- **Driver static state**: `AddScrinium` configures the driver statics of the process — a `HierarchicalProxyTolerantDiscriminatorConvention` on `typeof(object)`, a `SerializationContextAccessor`, and the enum string convention pack on the global convention registry — since the driver resolves them through static entry points that its internals invoke. They all read the ambient db context engine through the `IExecutionContext` that the application registers, resolved from its service provider when the first engine is built (`DeferredExecutionContext`): the registration extension never builds a service provider of its own. Their effect on the other consumers of the driver fork living in the same process is bounded: the discriminator convention, inherited by every type of the process, serves the types of a flow without an ambient engine with the driver convention they would get without the registration (the object one for `object` and interfaces, the hierarchical one otherwise), and the enum convention pack applies only to the class maps built inside a `MapsRegistrationHandler` scope, so any other type automapped in the process keeps the driver representation. What stays process wide is the serializer registry rerouting: inside a Scrinium scope every static `BsonSerializer.LookupSerializer` resolves on the ambient engine registry, so a foreign type whose class map member serializers are first resolved there binds them to Scrinium adapters for the life of the process. Pinned by `DriverStaticStateTests`.
- **DbContext engines are singletons, DbContext instances are scoped**: each DI scope (request, Hangfire job) gets its own `DbContext` instance, attached with `AttachToEngine` to the singleton `DbContextEngine` of its type (registered as keyed singleton `IDbContextEngine`, keyed by the db context type). Process-wide state (exclusive access flags, seeding state, registries, connections) lives on the engine; scoped instances own their repositories and unit of work state.
- **Exclusive access locking**: `RunWithExclusiveAccessAsync` (used by seeding and migrations) sets `IsExclusiveReadEnabled`/`IsExclusiveWriteEnabled`; while set, `LimitedAccessMongoCollection` throws `UnauthorizedAccessException` for any flow not holding an `ExclusiveAccessHandler` of that engine. The flags deny only the operations starting after the window opens, so the window also drains the ones admitted a moment before: every guarded operation counts in flight on the engine for the span of its forwarded call — entering before the flags read, so an operation admitted with the flags off is always visible to the drain — and the window waits for the counted operations to complete before running its work, writes always, reads only when locked (left open they keep flowing during the whole window, with no zero to wait for). The operations admitted by an exclusive access allowance don't stay counted, being the ones meant to work during the window, and the cursors already handed out iterate past the drain, like the traffic of the other processes proceeds. A drain unable to complete within `DbContextOptions.ExclusiveAccessDrainTimeout` (5 minutes by default) denies the exclusive work with a detailed `TimeoutException`: only an operation running for the whole timeout leaves it in flight, and the exclusive work running beside it would break the exclusivity the window declares. The allowance is scoped to the engine that granted it: the execution context items are shared by every db context of the flow (an HTTP request touching more db contexts, for instance), while the lock is per engine, so an allowance never opens a collection of another engine. Anything that must keep working during a migration (e.g. migration status reads for the dashboard) has to create its own handler scope, on the engine it addresses. Dependencies update tasks never hold a handler: executed during an exclusive access they fail, and the task executor retry converges them on the post exclusive state — background propagation never interleaves with migrations. The in-process window only serializes flows of one process: the cross process claim is the db context lock (`IDbContextEngine.DbContextLock`). `TryStartMigrationAsync` claims it with the new operation as owner — atomic on the server, so concurrent starts from any process elect a single operation, the losers delete themselves returning null, and the migration operations orphaned by dead owners close at the next start, once their lease expires — the migration execution resumes the claim, keeping the lease renewed and releasing it at completion (a lost lease cancels the running steps), and seeding claims the lock around its whole flow, waiting while another owner holds it and re-reading the seeding state from the db, up to the `SeedIfNeededAsync(lockWaitTimeout)` argument, so a dead owner's lease always expires inside the wait, and only a live owner working longer than it fails the seeding (the startup `SeedDbContexts(lockWaitTimeout)` forwards its own argument, and blocks on the seedings, so an owner never releasing fails them instead of hanging the startup). The claimed lease duration is per operation too: `TryStartMigrationAsync(lockLeaseDuration)` — covering also the window between the start and the background task runner picking the operation up, the only one nothing renews it — and `SeedIfNeededAsync(lockWaitTimeout, lockLeaseDuration)`, forwarded by `SeedDbContexts(lockWaitTimeout, lockLeaseDuration)`, both defaulted to `ResourceLock.DefaultLeaseDuration`; an unspecified seeding wait defaults to the lease duration in effect for that call. The lock doesn't extend the read/write denial to the other instances: their regular collection traffic proceeds during a migration run elsewhere.
- **Read-only access**: `DbContextOptions.IsReadOnly` denies any write from the whole db context, `RepositoryOptions.IsReadOnly` from a single repository (`IRepository.IsReadOnly` reports the effective flag, and repositories can coexist with different flags on the same db context). Useful to consume collections owned by another application. Enforcement lives on `LimitedAccessMongoCollection` and the wrappers it hands out (`LimitedAccessMongoIndexManager`, `LimitedAccessMongoSearchIndexManager`; `LimitedAccessMongoDatabase` for the `Database` property, with guarded collection retrieval and database level writes denied — commands included, since an arbitrary command can write; `LimitedAccessFilteredMongoCollection` for the `OfType` filtered views): every write operation on a read-only collection throws `UnauthorizedAccessException`, index and search index management included (creations, drops and updates are writes; listings stay reads), while reads work normally. An aggregation whose rendered pipeline ends in a `$out` or `$merge` stage, and a map reduce with output options other than inline, count as writes: both make the server write the results into a named collection, possibly in another database. No driver object reached from a guarded collection escapes the guards, except the raw driver client behind the explicit escape hatches `IMongoDatabase.Client` and `IDbContextEngine.Client`. A read-only db context also skips seeding (`SeedIfNeededAsync` logs and returns false) and denies migrations (`TryStartMigrationAsync` returns null, executing throws); migrations of a writable db context skip the index steps of its read-only repositories, whose indexes belong to the collection owner. The admin dashboard renders a read-only db context as a static card with a `Read-only` badge, without migration controls or status polling.
- **Transactions** (`IDbContext.ExecuteInTransactionAsync`): starts a transaction on a new session of the engine and registers it as ambient for the flow with a `DbSessionHandler` scope; `LimitedAccessMongoCollection` enlists in the ambient session every operation invoked without an explicit session on collections of the same engine (LINQ queries included — the LINQ3 provider executes through the public `Aggregate*` methods of the wrapped collection), so repository CRUD, queries and `SaveChangesAsync` inside the callback are transactional. Commit on callback completion, abort on exception, no automatic retry (a retry would replay a unit of work whose in-memory state was already consumed by the first attempt). Scoped to the engine connection: other db contexts (children included) don't enlist; change stream watches and estimated document counts stay session-less (not allowed in transactions); dependencies update tasks enqueued by saves are not transactional (after an abort they converge to the committed state). Requires a replica set or sharded cluster; sessions don't support concurrent operations inside the callback. With `DbContextOptions.EnableTransactionsWithReplicaSet` (default true) and a deployment supporting transactions — detected at runtime from the cluster topology (`IDbContextEngine.SupportsTransactions`: replica set, sharded, or load balanced; standalone servers keep plain saves with no configuration needed) — `SaveChangesAsync` saves its changed models into an implicit transaction of its own (skipped with no changed models, or when a session is already ambient — the saves enlist in it instead of nesting): atomic unit of work flushes without touching application code; set the option false to opt out.
- **`ConfigureAwait(false)` is required** on every awaited call in library code — these are libraries with no synchronization context to preserve.
- **Logging** uses strongly-typed `LoggerMessage.Define` delegates in each project's `Extensions/LoggerExtensions.cs`, grouped by level, with incremental never-reused event ids; the header comment `Last event id is: N` is the source of truth for the next free id — update it when adding a delegate. The repository logs of document operations (create, find, replace, delete, changed members save) and the dependencies propagation task logs write their document ids at `Debug`: entity keys can be natural identifiers — an email, a username, a wallet address — and the levels enabled by default report what happened, with repository and db context names, counts and outcomes, without reporting on which document it happened. The driver logs on its own `MongoDB.*` categories (`MongoDB.Command`, `MongoDB.Connection`, `MongoDB.SDAM`, `MongoDB.ServerSelection`, `MongoDB.Client`), through the host `ILoggerFactory` attached to the client cluster by `AddScrinium` with no category or level restriction: enabling `Debug` on `MongoDB.Command` logs the command bodies — query filters, inserted and updated documents — truncated to `LoggingSettings.MaxDocumentSize` (1000 characters by default). Keep those categories at their default level where document content must not reach the logs.
- Every source file starts with the standard **LGPL** copyright header (see any existing file); the solution license is GNU LGPL, stated by the `COPYING` + `COPYING.LESSER` text pair the GitHub detector reads as LGPL-3.0 (it recognises the lesser terms by that exact file name), and declared to the package registries by the `PackageLicenseExpression` of `Directory.Build.props` — `LGPL-3.0-or-later`, the SPDX form of the header wording, set once for every package and mutually exclusive with `PackageLicenseFile`, which no project sets. Both texts are packed as package content.
- **Brand icon**: every package declares `PackageIcon` over the root `icon.png` (the Scrinium symbol, ink on transparent, 256 px), packed as content beside the license texts and the readme; the same symbol is the tab icon of the dashboard and of the sample (`favicon.ico` plus the color scheme aware `favicon.svg`, in each `wwwroot/`). The files are derived from the brand symbol, whose parametric source lives with the marketing material (`assets/brand/scrinium/`): a change to the artwork regenerates them from there, never by editing the derived files.

## Issue tracker

Bugs and features are tracked in Jira project **SCR** (https://etherna.atlassian.net/projects/SCR). Branch names follow `feature/SCR-<id>-<slug>` / `improve/SCR-<id>-<slug>` / `fix/SCR-<id>-<slug>` — match this when creating branches.

## Release process and current line

Releases follow git flow: `dev` is merged into `main` with a merge commit, the merge commit is tagged `vX.Y.Z` and the tag push triggers the stable NuGet deploy (GitVersion derives the package version from the tag). The release is completed by a GitHub release carrying the notes of the Jira fixVersion, by the wiki flipped to *released* and tagged with the same tag, and by the public announcement. **0.25.0** (2026-09-03) is the first supported release and the first one under the Scrinium name: it removed DbCache and rebuilt the db context lifecycle (scoped contexts over singleton engines, per-instance changed models and identity map, member level saves, source repositories). `dev` continues that line; every issue follows the same workflow: branch from `dev` (`improve/SCR-xxx-*`, `feature/SCR-xxx-*` or `fix/SCR-xxx-*`), tests and AGENTS aligned in the same change, commits only after human review, one PR per issue to `dev`.

After the library release, the Etherna services must be migrated to 0.25.0: see [SERVICES-MIGRATION.md](SERVICES-MIGRATION.md) for the breaking changes summary, the per-service critical points (captive singleton consumers, root provider resolutions, identity map growth spots, test helper rewrites) and the live integration test checklist gating the services' bumps.

# Coding Style

## General Principles

- Keep commits clean: only include changes strictly necessary for the task at hand.
- Keep `README.md` aligned: when a change touches configuration, features, build/run steps, or architecture, update `README.md` in the same change.
- Keep the documentation wiki aligned: user-facing docs live in the sibling `scrinium.wiki` repo (published at https://github.com/Etherna/scrinium/wiki, checked out next to this one). Every substantial change to the public API, configuration, features, or architecture must be reported there in the same work line. The wiki currently documents the 0.25.0 API; see its own `AGENTS.md` for its conventions.
- Never reference AI agents or assistants in commits or code — no agent names, no `Co-Authored-By` agent trailers, no "generated/assisted by" notes. Commit messages and code must read as the team's own work.
- Exceptions to these conventions are accepted when strictly necessary or when they significantly improve code quality. Justify with a comment where needed.
- All elements (usings, properties, methods, fields, enum members, etc.) are always alphabetically ordered within their respective sections. Enum members with explicit values are the exception: they are declared in the order of their numbering, which carries the meaning the values encode (e.g. `ReactionMode`, whose values order by strictness).
- Primary constructors are preferred everywhere the constructor is a simple parameter assignment.
- Keep code clean: remove unused variables, dead code, and redundant imports.

## Naming

- **Classes/Structs**: PascalCase (`DbMigrationManager`, `ModelMap`)
- **Interfaces**: `I` prefix (`IDbContext`, `ITaskRunner`)
- **Async methods**: always `Async` suffix (`SaveChangesAsync`, `FindOneAsync`)
- **Properties**: PascalCase, with `Is`/`Has` prefix for booleans (`IsSeeded`, `CurrentStatus`)
- **Private fields**: `_camelCase` only when backing a same-named property (`_isSeeded` for `IsSeeded`); otherwise plain `camelCase`
- **Primary constructor parameters**: `camelCase` without underscore
- **Constants**: PascalCase (`HandlerKey`)
- **Enums**: PascalCase type and members (`ExecutionState.Succeded`)
- **Namespaces**: root namespace of the project + folder path (e.g. `Etherna.Scrinium.Core.Utility`); note `Scrinium.Hangfire` uses the `Etherna.Scrinium.HF` root
- **Custom exceptions**: `Scrinium` prefix and `Exception` suffix (`ScriniumEntityNotFoundException`), under `Exceptions/`

## Code Organization

- One class per file, filename matches class name
- Namespace mirrors folder structure (under the project's root namespace)
- Block-scoped namespaces: `namespace X { ... }` — NOT file-scoped
- Using directives outside the namespace block, always alphabetically ordered and kept to the minimum necessary
- No global usings — each file declares its own imports

## Comments

Principal comments (generally multiline, important):
```csharp
// Capital start, ending period.
// Continued on next line if needed.
```

Secondary/separator comments:
```csharp
//no space, no capital, no ending period
```

Longer explanations of non-obvious behavior use `/* ... */` blocks. Comment only what helps a future reader: non-obvious behavior, intent, or a gotcha. Do **not** write narration of your own reasoning or decisions — that belongs in the commit message / PR description, never in committed code. Comments and docs describe the **present** state only: never reference removed or replaced concepts ("legacy X", "previously done by Y") — git history already tells that story.

Public API members are documented with XML doc comments (`///`), with a well-composed description and no superfluous information; document non-public members too whenever it aids understanding.

## Member Ordering Within a Class

Use principal-style section comments to delimit groups, in this order:

```csharp
// Consts.
public const int MaxLength = 100;

// Fields.
private List<Item> _items = [];

// Constructors.
public MyType(string name) { ... }

// Initializer.
public void Initialize(...) { ... }

// Properties.
public string Name { get; }

// Methods.
public void DoSomething() { ... }

// Internals.
Task IInternalContract.DoInternalAsync() { ... }

// Helpers.
private void InternalHelper() { ... }
```

Internal facing members (explicit implementations of internal interfaces, internal methods) never mix with the public ones: they go in their own `// Internals.` section, after the public methods and before the helpers.

Private properties never mix with the public ones either: they go in their own `// Private properties.` section, right after `// Properties.`.

## Class Design

- `internal sealed` for implementations that aren't part of the public API
- Primary constructors everywhere the constructor is a simple assignment
- Don't extract a private helper method for logic used in a single place — inline it. Reserve helpers for code shared by two or more call sites (or when extraction materially clarifies an otherwise long, complex method)
- Framework-initialized components implement `IDbContextEngineInitializable` (engine level: registries, maintainer, migration manager) or `IDbContextInitializable` (scope level: repositories and their registry) — `Initialize(..., logger)` + `IsInitialized` guard instead of taking the dependency in the constructor

### Persisted Model Classes

- `virtual` on all properties and methods for proxy support
- Protected parameterless constructor for deserialization
- Collection encapsulation with a private backing field exposed as `IEnumerable<T>`, never `null` (at most empty)
- No manual lazy-load annotations: the proxy models source generator analyzes each domain method body and computes the properties it alters (direct backing field accesses included, following non virtual helpers), triggering the full load when such a method runs on a summary model. A method without analyzable source (compiled cross-assembly bases) conservatively full loads on summaries. Collections stay encapsulated (read-only interface, non public setter), mutated only through domain methods, so a snapshot diff always detects the change
- Prefer immutable exposure. A getter that hands out mutable state (a `List<T>`/`Dictionary<>`, or a complex value with public setters or business methods) is legal, but **reading it flags the model for a diff at save** — a change could otherwise escape interception. Expose collections read-only and make embedded value objects immutable (records, get/init-only, no mutating methods) to keep reads free; entity references never count (tracked on their own repository). `ProxyModels/MutabilityAnalyzer` computes this; casting a read-only collection back to mutable to bypass it is unsupported

## Async Patterns

- Always suffix with `Async`
- `CancellationToken cancellationToken = default` as the optional last parameter (non-nullable, `default` — not `CancellationToken? = null`)
- Return `Task` or `Task<T>`, never `async void`
- `ConfigureAwait(false)` on **every** awaited call in library code

## Null Handling

- Nullable reference types enabled (`<Nullable>enable</Nullable>`)
- `ArgumentNullException.ThrowIfNull(param)` for parameter validation — no explicit `nameof`, the parameter name is captured automatically
- `is null` / `is not null` (not `== null`)
- Prefer `null` over `default` wherever the type admits it: optional parameter defaults, late-init member initializers (`= null!`, not `= default!`), returns and assignments. Keep `default` only where `null` can't apply: non-nullable value types (e.g. `CancellationToken cancellationToken = default`) and unconstrained generic type parameters.
- `= null!` on non-nullable members assigned after construction (e.g. by `Initialize`)
- `??` and `??=` operators

## Formatting

- Allman braces (opening brace on new line)
- 4-space indentation
- Expression-bodied members for single expressions
- LINQ method chains: one operation per line, aligned
- Blank line between member sections

## C# Language Features

- Pattern matching: `is`, `is not`, type patterns, property patterns
- Switch expressions for multi-branch returns
- Primary constructors everywhere applicable
- Collection expressions: `[]`, `[..spread]`
- Prefer collection expressions over constructors to initialize any collection (lists, arrays, dictionaries, etc.): `[]` not `new()`, `["a", "b"]` not `new List<string> { "a", "b" }`. Use a constructor only when a collection expression can't express the intent (e.g. presizing capacity with `new List<T>(capacity)`, or building a specific set type like `new HashSet<T>(value ?? [])`). This applies also where the surrounding legacy code still uses constructors: new code follows the rule, not the neighbors.
- Target-typed `new()` when type is clear from context (for non-collection types)
- Tuple deconstruction for multiple return values
- Prefer a property pattern over a chain of `&&` combining a type/null check with member accesses: it expresses the condition as a single declarative shape the value must match, rather than an imperative sequence of checks. This applies also to pure boolean member chains on the same value, with no type/null check involved: `field is { IsInitOnly: false, IsLiteral: false }`, not `!field.IsInitOnly && !field.IsLiteral`
- `field` keyword for field-backed properties (e.g. lazy initialization) instead of an explicit backing field: `public T Prop => field ??= Compute();`. Needs C# 14: in `src/` it applies only once the `net8.0`/`net9.0` targets are dropped (each TFM compiles with its default LangVersion); the net10-only test projects can use it today
- Lock fields: prefer the dedicated `System.Threading.Lock` type (.NET 9+) over a plain `object` — more expressive, and the compiler enforces correct `lock` usage on it. In `src/` it applies only once the `net8.0` target is dropped (lowest-target rule); the net10-only test projects can use it today

## LINQ

- Method syntax preferred over query syntax
- Query syntax only for complex join/groupby with multiple `from` clauses
- Fluent chaining, one operation per line for readability

## Testing (xUnit + Moq)

- AAA pattern with section comments: `// Setup.`, `// Action.`, `// Assert.` (collapse when a single statement covers two phases)
- `[Fact]` for basic tests, `[Theory]` with `[InlineData]`/`[MemberData]` for parameterized cases
- xUnit assertions: `Assert.Equal()`, `Assert.NotNull()`, `Assert.ThrowsAsync<T>()`
- Moq for mocking: `new Mock<IDbContext>()`
- **No `ConfigureAwait` in test code** — inside tests write plain `await foo()`; the library rule applies to `src/` only
- The test project mirrors the `Scrinium.Core` layout
